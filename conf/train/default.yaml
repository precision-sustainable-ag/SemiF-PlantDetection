tasks:
  - prepare_dataset
  - train_model

model_name: yolo11n.pt
epochs: 5
image_size: 640
batch_size: 1
# num_workers: 10
# patience:
# lr:
# weight_decay:

random_seed: 42
validation_split: 0.2
parallel: true
parallel_workers: 16

# Model hyperparameters
# patience: 50
# lr: 0.001
# weight_decay: 0.0005

prepare_dataset:
  remove_after_split: false # removes images and annotations after creating train/val/test splits

train_from_checkpoint: true